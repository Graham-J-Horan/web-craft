name = "webcraft-ai"
compatibility_date = "2024-11-01"
compatibility_flags = ["nodejs_compat"]

# We leave 'main' and 'bucket' out here to avoid build-time validation errors.
# Cloudflare dashboard settings will handle the paths.
